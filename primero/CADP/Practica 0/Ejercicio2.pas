program Ejercicio2a;
var
  num1, num2, suma: integer;
begin
  read(num1);
  read(num2);
  suma:= num1 + num2;
  write(suma);
end.

{program Ejercicio2b;
var
  num1, num2: integer;
begin
  read(num1);
  read(num2);
  write(num1+num2);
end.}
